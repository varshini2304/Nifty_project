# docker-compose.prod.yml
version: '3.9'

services:
  backend:
    environment:
      NODE_ENV: production
      CORS_ORIGIN: http://localhost:3000

  frontend:
    build:
      args:
        VITE_API_BASE_URL: http://localhost:5000/api/v1

  batch:
    environment:
      NODE_ENV: production
